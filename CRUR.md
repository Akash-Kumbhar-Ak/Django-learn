# Django CRUD Operations using ORM (Shell)

This document explains how to perform **CRUD (Create, Read, Update, Delete)** operations in Django using the **Django ORM** via the **Django Shell**.

---

## 📌 Prerequisites
- Python installed
- Django installed
- Django project & app created
- Model migrated to database

---

## 🐚 Open Django Shell

Run the following command from the project root:

```bash
python manage.py shell
